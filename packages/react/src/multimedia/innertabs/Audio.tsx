import { MediaLibraryResponse } from "../MediaLibrary";

export const Audio = ({ onSuccess }: { onSuccess: MediaLibraryResponse }) => {
  return (
    <p>
      TODO: Audio <button onClick={onSuccess}>successful</button>
    </p>
  );
};
