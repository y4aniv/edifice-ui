// @ts-ignore
import { Meta } from "@storybook/react";
import useBookmark from "./useBookmark";
import docs from "./useBookmark";

const meta: Meta<typeof useBookmark> = {
  title: "Core/useBookmark",
  parameters: {
    docs: { page: docs },
  },
};

export default meta;
